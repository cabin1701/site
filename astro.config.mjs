// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://cabin1701.github.io',
  base: '/cabin1701-site-template',
  trailingSlash: 'always',
});
